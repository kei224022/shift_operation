Java シフト演算プログラム

入力した数字を8桁の2進数に変換し、追加で入力した数(正なら左シフト、負なら右シフト)だけシフト演算を行い、
計算結果を10進数で表示するプログラム

javaのシフト演算子("<<", ">>")を使用すると簡単にできてしまうため、DecimalToBinaryクラスBinaryToDecimalクラス
を使わせることで自力でやることになる。

クラス、メソッド、インスタンス、例外処理について理解できるはず

制限:
入力は255以下の正の整数 (256以上を入力した場合は例外処理)
シフト演算時に256以上になる場合は例外処理
